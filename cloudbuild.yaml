steps:
  # 1. Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-image'
    args:
      - 'build'
      - '-t'
      - '${_LOCATION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_REPO_NAME}:$COMMIT_SHA'
      - '-t'
      - '${_LOCATION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_REPO_NAME}:latest'
      - '.'

  # 2. Push image tags to Artifact Registry
  #    (gcr.io/Container Registry is deprecated; Artifact Registry is the
  #    supported GCP image store as of 2025.)
  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-sha-tag'
    args:
      - 'push'
      - '${_LOCATION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_REPO_NAME}:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-latest-tag'
    args:
      - 'push'
      - '${_LOCATION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_REPO_NAME}:latest'

  # 3. Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy-to-cloud-run'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - '${_SERVICE_NAME}'
      - '--image'
      - '${_LOCATION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_REPO_NAME}:$COMMIT_SHA'
      - '--region'
      - '${_LOCATION}'
      # Cloud Run's default PORT (8080) matches server.ts now; no --port override needed.
      - '--allow-unauthenticated'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--concurrency'
      - '80'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '4'
      - '--timeout'
      - '60'
      # GEMINI_API_KEY is pulled from Secret Manager, never baked into the image.
      # Prereq: `gcloud secrets create GEMINI_API_KEY --data-file=-` and grant the
      # Cloud Run runtime service account roles/secretmanager.secretAccessor.
      - '--set-secrets'
      - 'GEMINI_API_KEY=GEMINI_API_KEY:latest'
      - '--set-env-vars'
      - 'ALLOWED_DOMAIN=${_ALLOWED_DOMAIN}'

substitutions:
  _REPO_NAME: 'uad36-wiki'
  _SERVICE_NAME: 'uad36-wiki'
  _LOCATION: 'us-central1'
  _AR_REPO: 'uad36-wiki-repo'
  _ALLOWED_DOMAIN: 'truefootage.tech'

images:
  - '${_LOCATION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_REPO_NAME}:$COMMIT_SHA'
  - '${_LOCATION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_REPO_NAME}:latest'

options:
  logging: CLOUD_LOGGING_ONLY
